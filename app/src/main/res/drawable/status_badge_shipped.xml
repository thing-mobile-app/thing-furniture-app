<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#BFDBFE" /> <!-- Saturated Blue -->
            <corners android:radius="20dp" />
            <stroke android:width="1.5dp" android:color="@color/status_shipped" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/status_shipped_bg" />
            <corners android:radius="20dp" />
            <stroke android:width="1dp" android:color="@color/status_shipped" />
        </shape>
    </item>
</selector>
